---
// src/pages/noticias/[slug].astro
import MainLayout from "../../layouts/MainLayout.astro";
import { getSiteInfo, getNoticiaBySlug, getNoticias } from "../../lib/wordpress";

export async function getStaticPaths() {
  const { noticias } = await getNoticias({ limit: 20000 });
  return noticias.map((n) => ({ params: { slug: n.slug }, props: { noticia: n } }));
}

const { slug } = Astro.params;
const { noticia } = Astro.props;

const siteInfo = await getSiteInfo();
const data = noticia || (await getNoticiaBySlug(slug));
if (!data) {
  return Astro.redirect("/404");
}

// Helper para formatear fecha
const formatDate = (dateString) => {
  if (!dateString) return '';
  const date = new Date(dateString);
  return date.toLocaleDateString('es-ES', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
    hour: '2-digit',
    minute: '2-digit'
  });
};

// Helper para calcular tiempo de lectura
const calculateReadTime = (content) => {
  if (!content) return '1 min';
  const wordsPerMinute = 200;
  const words = content.replace(/<[^>]*>/g, '').split(/\s+/).length;
  const minutes = Math.ceil(words / wordsPerMinute);
  return `${minutes} min${minutes > 1 ? '' : ''}`;
};

const imagenNoticia =
  data.imagenDestacadaUrl ||
  data.featuredImage?.node?.sourceUrl ||
  '/images/placeholder-news.jpg';

const altImagen =
  data.featuredImage?.node?.altText ||
  data.title;

const pageTitle = data.seo?.title || `${data.title} - VinylStation`;
const pageDescription =
  data.seo?.metaDesc ||
  data.excerpt ||
  `Lee "${data.title}", última noticia en VinylStation.`;

const fechaPublicacion = formatDate(data.date);
const tiempoLectura = calculateReadTime(data.content);
const categorias = data.categorias?.nodes || [];
const autor = data.author?.node;
---
<MainLayout title={`${pageTitle} | ${siteInfo.title}`} description={pageDescription}>
  <div class="noticia-page">
    <nav class="breadcrumbs">
      <div class="container">
        <a href="/">Inicio</a>
        <span class="separator">›</span>
        <a href="/noticias">Noticias</a>
        <span class="separator">›</span>
        <span class="current">{data.title}</span>
      </div>
    </nav>

    <article class="noticia-details">
      <div class="container">
        
        <!-- Header de la noticia -->
        <header class="noticia-header">
          <div class="noticia-meta-top">
            {categorias.length > 0 && (
              <div class="noticia-categories">
                {categorias.map((cat) => (
                  <span class="category-badge" key={cat.id}>{cat.name}</span>
                ))}
              </div>
            )}
            <div class="noticia-date-reading">
              <time class="noticia-date">{fechaPublicacion}</time>
              <span class="reading-time">⏱️ {tiempoLectura} lectura</span>
            </div>
          </div>
          
          <h1 class="noticia-title">{data.title}</h1>
          
          {data.excerpt && (
            <div class="noticia-excerpt" set:html={data.excerpt}></div>
          )}
          
          {autor && (
            <div class="noticia-author">
              <div class="author-info">
                {autor.avatar?.url && (
                  <img 
                    src={autor.avatar.url} 
                    alt={autor.name}
                    class="author-avatar"
                  />
                )}
                <div class="author-details">
                  <span class="author-name">Por {autor.name}</span>
                  {autor.description && (
                    <span class="author-description">{autor.description}</span>
                  )}
                </div>
              </div>
            </div>
          )}
        </header>

        <!-- Imagen destacada -->
        <div class="noticia-featured-image">
          <img
            src={imagenNoticia}
            alt={altImagen}
            loading="lazy"
            onerror="this.onerror=null;this.src='/images/placeholder-news.jpg';"
          />
          {data.featuredImage?.node?.caption && (
            <div class="image-caption" set:html={data.featuredImage.node.caption}></div>
          )}
        </div>

        <!-- Contenido de la noticia -->
        <div class="noticia-content">
          {data.content ? (
            <div class="content-body" set:html={data.content}></div>
          ) : (
            <p class="no-content">El contenido de esta noticia no está disponible.</p>
          )}
        </div>

        <!-- Footer de la noticia -->
        <footer class="noticia-footer">
          <div class="share-section">
            <h4>Compartir esta noticia</h4>
            <div class="share-buttons">
              <a 
                href={`https://twitter.com/intent/tweet?text=${encodeURIComponent(data.title)}&url=${encodeURIComponent(Astro.url.toString())}`}
                target="_blank"
                rel="noopener noreferrer"
                class="share-btn twitter"
              >
                📱 Twitter
              </a>
              <a 
                href={`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(Astro.url.toString())}`}
                target="_blank"
                rel="noopener noreferrer"
                class="share-btn facebook"
              >
                📘 Facebook
              </a>
              <a 
                href={`https://wa.me/?text=${encodeURIComponent(data.title + ' ' + Astro.url.toString())}`}
                target="_blank"
                rel="noopener noreferrer"
                class="share-btn whatsapp"
              >
                💬 WhatsApp
              </a>
            </div>
          </div>

          <div class="noticia-actions">
            <a href="/noticias" class="btn-back-noticias">
              ← Volver a todas las noticias
            </a>
          </div>
        </footer>
      </div>
    </article>

    <!-- Noticias relacionadas (opcional, se puede implementar más tarde) -->
    <!--
    <section class="noticias-relacionadas">
      <div class="container">
        <h2 class="section-title">Noticias Relacionadas</h2>
        <div class="related-news-grid">
          // Aquí se pueden mostrar noticias de la misma categoría
        </div>
      </div>
    </section>
    -->
  </div>
</MainLayout>

<style>
  /* ==== Contenedor principal ==== */
  .noticia-page {
    min-height: 100vh;
    background: #0a0a0a;
    color: #e0e0e0;
    padding-bottom: 3rem;
  }

  /* ==== Breadcrumbs ==== */
  .breadcrumbs {
    background: #111;
    padding: 1rem 0;
    border-bottom: 1px solid #222;
  }

  .breadcrumbs .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.9rem;
  }

  .breadcrumbs a {
    color: #ccc;
    text-decoration: none;
    transition: color 0.3s ease;
  }

  .breadcrumbs a:hover {
    color: var(--color-primary, #ff3333);
  }

  .breadcrumbs .separator {
    color: #777;
  }

  .breadcrumbs .current {
    color: var(--color-primary, #ff3333);
    font-weight: 600;
  }

  /* ==== Contenedor principal de la noticia ==== */
  .noticia-details .container {
    max-width: 800px;
    margin: 0 auto;
    padding: 3rem 1rem;
  }

  /* ==== Header de la noticia ==== */
  .noticia-header {
    margin-bottom: 3rem;
  }

  .noticia-meta-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 2rem;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .noticia-categories {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .category-badge {
    background: var(--color-primary, #ff3333);
    color: white;
    padding: 0.4rem 1rem;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .noticia-date-reading {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.3rem;
    color: #888;
    font-size: 0.9rem;
  }

  .noticia-date {
    color: var(--color-primary, #ff3333);
    font-weight: 600;
  }

  .reading-time {
    color: #666;
    font-size: 0.8rem;
  }

  .noticia-title {
    font-size: 2.8rem;
    color: #fff;
    margin-bottom: 1.5rem;
    line-height: 1.2;
    font-weight: 800;
  }

  .noticia-excerpt {
    font-size: 1.2rem;
    color: #bbb;
    line-height: 1.6;
    margin-bottom: 2rem;
    padding: 1.5rem;
    background: rgba(255, 51, 51, 0.05);
    border-left: 4px solid var(--color-primary, #ff3333);
    border-radius: 0 8px 8px 0;
  }

  .noticia-author {
    padding: 1.5rem 0;
    border-top: 1px solid #333;
    border-bottom: 1px solid #333;
  }

  .author-info {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .author-avatar {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    object-fit: cover;
  }

  .author-details {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .author-name {
    color: #fff;
    font-weight: 600;
    font-size: 1rem;
  }

  .author-description {
    color: #888;
    font-size: 0.9rem;
  }

  /* ==== Imagen destacada ==== */
  .noticia-featured-image {
    margin: 3rem 0;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 8px 30px rgba(0,0,0,0.7);
  }

  .noticia-featured-image img {
    width: 100%;
    height: auto;
    display: block;
  }

  .image-caption {
    background: #1a1a1a;
    padding: 1rem;
    color: #888;
    font-size: 0.9rem;
    font-style: italic;
    text-align: center;
  }

  /* ==== Contenido de la noticia ==== */
  .noticia-content {
    margin: 3rem 0;
  }

  .content-body {
    color: #e0e0e0;
    line-height: 1.8;
    font-size: 1.1rem;
  }

  /* ==== FUERZA BRUTA PARA ENCABEZADOS ==== */
  .noticia-page .noticia-details .container .noticia-content .content-body h2,
  .content-body h2,
  article h2,
  div h2 {
    font-size: 2.8rem !important;
    color: #ffffff !important;
    font-weight: 800 !important;
    border-bottom: 4px solid #ff3333 !important;
    padding-bottom: 1.2rem !important;
    margin: 3rem 0 2rem 0 !important;
    text-shadow: 0 2px 6px rgba(0,0,0,0.7) !important;
    line-height: 1.2 !important;
    display: block !important;
  }

  .noticia-page .noticia-details .container .noticia-content .content-body h3,
  .content-body h3,
  article h3,
  div h3 {
    font-size: 2.3rem !important;
    color: #ff3333 !important;
    font-weight: 700 !important;
    margin: 2.5rem 0 1.8rem 0 !important;
    padding-left: 1.5rem !important;
    position: relative !important;
    text-shadow: 0 1px 4px rgba(0,0,0,0.8) !important;
    line-height: 1.3 !important;
    display: block !important;
  }

  .noticia-page .noticia-details .container .noticia-content .content-body h3::before,
  .content-body h3::before,
  article h3::before,
  div h3::before {
    content: '▎' !important;
    position: absolute !important;
    left: 0 !important;
    top: 0 !important;
    font-size: 2.3rem !important;
    color: #ff3333 !important;
    text-shadow: 0 0 10px rgba(255, 51, 51, 1) !important;
  }

  .noticia-page .noticia-details .container .noticia-content .content-body h4,
  .content-body h4,
  article h4,
  div h4 {
    font-size: 1.9rem !important;
    color: #ffffff !important;
    font-weight: 600 !important;
    background: linear-gradient(90deg, rgba(255, 51, 51, 0.15), transparent) !important;
    border-left: 6px solid #ff3333 !important;
    padding: 1.2rem 0 1.2rem 2rem !important;
    margin: 2.2rem 0 1.6rem 0 !important;
    border-radius: 0 12px 12px 0 !important;
    line-height: 1.3 !important;
    display: block !important;
  }

  .content-body p {
    margin: 1.5rem 0;
    text-align: justify;
  }

  .content-body a {
    color: var(--color-primary, #ff3333);
    text-decoration: underline;
    transition: opacity 0.3s ease;
  }

  .content-body a:hover {
    opacity: 0.8;
  }

  .content-body blockquote {
    background: #1a1a1a;
    border-left: 4px solid var(--color-primary, #ff3333);
    padding: 1.5rem;
    margin: 2rem 0;
    border-radius: 0 8px 8px 0;
    font-style: italic;
    color: #bbb;
  }

  .content-body ul,
  .content-body ol {
    margin: 1.5rem 0;
    padding-left: 2rem;
  }

  .content-body li {
    margin: 0.5rem 0;
  }

  .content-body img {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
    margin: 2rem 0;
    box-shadow: 0 4px 15px rgba(0,0,0,0.5);
  }

  .no-content {
    text-align: center;
    color: #777;
    font-style: italic;
    padding: 3rem;
    background: #1a1a1a;
    border-radius: 8px;
  }

  /* ==== Footer de la noticia ==== */
  .noticia-footer {
    margin-top: 4rem;
    padding-top: 2rem;
    border-top: 2px solid #333;
  }

  .share-section {
    margin-bottom: 2rem;
  }

  .share-section h4 {
    color: #fff;
    font-size: 1.2rem;
    margin-bottom: 1rem;
  }

  .share-buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .share-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.8rem 1.5rem;
    border-radius: 25px;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.9rem;
    transition: all 0.3s ease;
  }

  .share-btn.twitter {
    background: #1da1f2;
    color: white;
  }

  .share-btn.facebook {
    background: #4267b2;
    color: white;
  }

  .share-btn.whatsapp {
    background: #25d366;
    color: white;
  }

  .share-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(0,0,0,0.3);
  }

  /* ==== Botón "volver" ==== */
  .noticia-actions {
    text-align: center;
    padding-top: 2rem;
    border-top: 1px solid #333;
  }

  .btn-back-noticias {
    background: var(--color-primary, #ff3333);
    color: #fff;
    padding: 1rem 2rem;
    border-radius: 30px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .btn-back-noticias:hover {
    background: #cc2929;
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(255, 51, 51, 0.3);
  }

  /* ==== Responsive ==== */
  @media (max-width: 768px) {
    .noticia-details .container {
      padding: 2rem 1rem;
    }

    .noticia-title {
      font-size: 2.2rem;
    }

    .noticia-excerpt {
      font-size: 1.1rem;
      padding: 1rem;
    }

    .noticia-meta-top {
      flex-direction: column;
      align-items: flex-start;
    }

    .noticia-date-reading {
      align-items: flex-start;
    }

    .share-buttons {
      justify-content: center;
    }

    .share-btn {
      padding: 0.7rem 1.2rem;
      font-size: 0.8rem;
    }

    .content-body {
      font-size: 1rem;
    }

    .content-body h1 {
      font-size: 2rem !important;
    }

    .content-body h2 {
      font-size: 1.8rem !important;
    }

    .content-body h3 {
      font-size: 1.6rem !important;
    }

    .content-body h4 {
      font-size: 1.4rem !important;
    }

    .content-body h5 {
      font-size: 1.2rem !important;
    }

    .content-body h6 {
      font-size: 1.1rem !important;
    }
  }

  @media (max-width: 480px) {
    .noticia-title {
      font-size: 1.8rem;
    }

    .noticia-excerpt {
      font-size: 1rem;
    }

    .breadcrumbs .current {
      display: none;
    }

    .share-buttons {
      flex-direction: column;
      align-items: center;
    }

    .share-btn {
      width: 200px;
      justify-content: center;
    }

    .content-body h1 {
      font-size: 1.8rem !important;
    }

    .content-body h2 {
      font-size: 1.6rem !important;
    }

    .content-body h3 {
      font-size: 1.4rem !important;
    }

    .content-body h4 {
      font-size: 1.3rem !important;
    }

    .content-body h5 {
      font-size: 1.1rem !important;
    }

    .content-body h6 {
      font-size: 1rem !important;
    }
  }

  /* Animación de entrada */
  .noticia-header {
    animation: fadeInUp 0.8s ease-out;
  }

  .noticia-featured-image {
    animation: fadeInUp 0.8s ease-out 0.2s both;
  }

  .noticia-content {
    animation: fadeInUp 0.8s ease-out 0.4s both;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
