import { unlink } from 'fs/promises';\nimport { existsSync } from 'fs';\n\n// Archivos de diagn\u00f3stico a eliminar\nconst filesToDelete = [\n  'debug-noticias-old.mjs',\n  'diagnostico-completo.mjs',\n  'diagnostico-corregido.mjs',\n  'fix-noticias-simple.mjs',\n  'CONFIGURACION-WORDPRESS.md'\n];\n\nconst basePath = 'C:\\\\Users\\\\nestu\\\\Desktop\\\\web vinylstation\\\\vinylstationV1.astro';\n\nfor (const file of filesToDelete) {\n  const fullPath = `${basePath}\\\\${file}`;\n  if (existsSync(fullPath)) {\n    try {\n      await unlink(fullPath);\n      console.log(`\u2705 Eliminado: ${file}`);\n    } catch (error) {\n      console.error(`\u274c Error eliminando ${file}:`, error.message);\n    }\n  } else {\n    console.log(`\u26a0\ufe0f No existe: ${file}`);\n  }\n}\n\nconsole.log('\\n\ud83e\uddf9 Limpieza completada');\n